import type { Meta, StoryObj } from "@storybook/react";

import { {{ pascalCase name }} } from "./component";

const meta = {
  title: "blocks/{{ titleCase name }}",
  component: {{ pascalCase name }},
  tags: ["autodocs"],
  parameters: { layout: "fullscreen" },
  args: {
    eyebrow: "Eyebrow",
    title: "{{ titleCase name }}",
    richText: [
      {
        _type: "block",
        _key: "block-1",
        style: "normal",
        children: [{ _type: "span", text: "Supporting copy for this section." }],
      },
    ],
  },
} satisfies Meta<typeof {{ pascalCase name }}>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};
