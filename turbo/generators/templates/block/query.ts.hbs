import { richTextFragment } from "../internal/groq-fragments";

/** Projection for the `{{ camelCase name }}` block inside `pageBuilder[]`. */
export const {{ camelCase name }}GroqProjection = /* groq */ `
  _type == "{{ camelCase name }}" => {
    ...,
    ${richTextFragment}
  }
`;
