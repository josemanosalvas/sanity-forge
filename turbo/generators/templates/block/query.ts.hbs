import { richTextFragment } from "../../lib/groq-fragments";

/** Projection for the `{{ camelCase name }}` block inside `pageBuilder[]`. */
export const {{ camelCase name }}GroqProjection =`
  _type == "{{ camelCase name }}" => {
    ...,
    ${richTextFragment}
  }
`;
