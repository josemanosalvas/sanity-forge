import type { NextConfig } from "next";

// Storybook's Next.js framework reads this to mirror the app's compiler
// behaviour (next/image, next/link, next/font).
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
