/**
 * The Sanity API version every part of the repository speaks: the Studio's
 * validations and structure, the Next.js clients and TypeGen. Pinned to a date
 * on which `perspective` defaults to `published` (API >= 2025-02-19).
 */
export const SANITY_API_VERSION = "2026-09-01";
